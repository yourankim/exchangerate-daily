name: daily-message
on:
  schedule:
    - cron: '10 11 * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
          curl --request GET \
          --url 'https://exhangerate-daily.vercel.app/api/cron' 